%{
%}

S               [ \t\v\n\r]
X               (ab|ba)b*(ab|ba)
Y               (aabb|bbaa)

%%

b*({X}|{Y})b*       printf("accepted: %s\n", yytext);
{S}*                { /* doing nothing */ }
.*                  printf("rejected: %s\n", yytext);

%%

int main(int argc, char *argv[]) {
    yylex();
    return 0;
}